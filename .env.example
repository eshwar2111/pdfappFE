# Base URL of the FastAPI backend.
# Left empty in development so requests go through the Vite proxy on the same
# origin. Set to the deployed API origin for a production build.
#
# NOTE: nothing secret belongs in this file. Vite inlines every VITE_* variable
# into the client bundle. LLM API keys live only in the backend environment.
VITE_API_BASE_URL=
